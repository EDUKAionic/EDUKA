import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ParrainFilleulsPage } from './parrain-filleuls.page';

describe('ParrainFilleulsPage', () => {
  let component: ParrainFilleulsPage;
  let fixture: ComponentFixture<ParrainFilleulsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ParrainFilleulsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
